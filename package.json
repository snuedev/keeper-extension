{
  "name": "keeper-extension",
  "version": "1.0.0",
  "description": "Keeper - a private place to keep your notes, from anywhere in the browser.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "watch": "vite build --watch",
    "clean": "rm -rf chrome-extension/dist"
  },
  "dependencies": {
    "firebase": "^12.16.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
